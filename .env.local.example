# Gyotaku local replica defaults. Copy to .env.local and adjust as needed.
# host: make start / dfx start --clean --background で起動するレプリカ。
GYOTAKU_IC_HOST=http://127.0.0.1:8080

# canister: `dfx canister id taggr` の出力を書く。初回は `make local_deploy` 後に取得する。
GYOTAKU_CANISTER_ID=replace-with-local-taggr-id

# bucket canisterに直接アクセスする際のURLテンプレート。通常は既定値のままでOK。
GYOTAKU_BUCKET_URL_TEMPLATE=https://{canister}.raw.icp0.io/asset

# Taggrのrealm/domain指定。必要なら書き換える。
GYOTAKU_TAGGR_DOMAIN=taggr
